{
  "framework": "nextjs",
  "functions": {
    "app/api/extract-recipe/route.ts": {
      "runtime": "nodejs18.x",
      "memory": 1024
    }
  },
  "buildCommand": "pnpm install && pip install -r requirements.txt && next build",
  "installCommand": "curl -sSL https://install.python-poetry.org | python3 - && pip install -r requirements.txt",
  "env": {
    "PYTHON_VERSION": "3.9",
    "PYTHONPATH": "/var/task"
  }
} 